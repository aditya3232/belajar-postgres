## Hal Unik dari PostgreSQL
```zsh
1. schemas -> namespace didalam database, atau pengelompokan tabel di satu database. nama tb yang sama bisa asal schema berbeda
2. check -> constrains waktu bikin tabel. berguna untuk validasi otomatis didalam database. beruna untuk validasi angka (>=, <=, range), validasi enum sederhana
3. autonumber (sequence, identity) -> kalau di mysql pakai auto_increment
    - identity generated always (tidak bisa input manual): id INT GENERATED ALWAYS AS IDENTITY PRIMARY KEY
    - identity by default (auto generate tapi boleh override, sama seperti auto_increment mysql): id INT GENERATED BY DEFAULT AS IDENTITY PRIARY KEY
    - sequence (perlu buat sequence nya dulu, nanti objek ini bisa dipakai dimana aja, dan bisa custom behavior, misal start 100, increment 5)
```

## non-relasional & relasional 
```zsh
1. non-relasional -> cenderung ada redudansi data 
2. relasional -> berusaha mengurangi redudansi data, dengan memisah data pada tabel berbeda, dan dihubugkan dengan relasi. mengurangi redudansi pakai konsep normalisasi
3. 1NF (first normal form) -> 1 kolom = 1 nilai
4. 2NF (second normal form) -> semua kolom harus bergantung ke seluruh primary key, bukan sebagian: ❌ [order_id | product_id | product_name]. harusnya product_name sudah di tb_product
5. 3NF (third normal form) -> 
```

## sql & nosql
```zsh
1. bentuk data nosql -> bisa json (mongoDB), dan key-value (redis) 
2. kapan pakai sql -> struktur data fix tidak berubah, butuh proses berurutan dan konsisten (ACID), data punya relasi jelas
3. kapan pakai nosql -> struktur data fleksibel misal data user bisa memiliki field beda-beda, traffic besar dan tidak butuh relasi, misal log system, cache
```
